name: Test action

on:
  push:
    branches:
      - main

jobs:
  bundle_dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build bundle
        uses: hedger/fz-bundler@main
        with:
          path: test/manifest.yml
          channel: dev
